services:
  bot:
    build: .
    container_name: smart-hr-concierge
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    command: python main.py
